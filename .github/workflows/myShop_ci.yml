name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Jenkins pipeline
        uses: technicalpickles/jenkins-webhook-action@v1
        with:
          url: 'http://127.0.0.1:8080/generic-webhook-trigger/invoke?token='